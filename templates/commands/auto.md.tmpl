---
description: Renombrado a /smart. Corre /smart con los mismos argumentos.
---

Este comando ahora se llama `/smart`. Corre `/smart $ARGUMENTS` tal cual: mismos
argumentos, mismo playbook, que vive en `/smart` y es el único que manda.

Se renombró porque `/auto` chocaba con el comando homónimo de otros agentes.
Este puntero se borra en el próximo ciclo.
